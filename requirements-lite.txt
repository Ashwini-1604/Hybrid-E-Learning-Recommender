# Lightweight requirements for RAM-constrained free-tier hosting (e.g. Render free = 512MB).
# Skips sentence-transformers/PyTorch entirely so the app runs on TF-IDF only.
# Also set an environment variable DISABLE_SEMANTIC=true on your host when using this file.
streamlit>=1.34
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
scipy>=1.11
